# Only the backend + frontend share code (packages/shared). The youtube.js
# provider is intentionally a STANDALONE pnpm project (its own lockfile) so it
# stays fully decoupled and gets a self-contained, reproducible Docker build.
packages:
  - "apps/backend"
  - "apps/frontend"
  - "packages/shared"
