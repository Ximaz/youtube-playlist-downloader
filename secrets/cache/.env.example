# Valkey ('cache' service): response cache + BullMQ queue + OAuth state.
# These provision the Valkey ACL user; the backend connects via CACHE_URL in secrets/backend/.env.
VALKEY_USERNAME=ypd
VALKEY_PASSWORD=ypd-dev-valkey-change-me
